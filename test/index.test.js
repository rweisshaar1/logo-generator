const functions = require('../index.js')

describe("Test of create logo function", () => {
  describe("creates a logo properly", () => {
    it("should take user response: if response is <= 3 characters, the function will work properly. Should console log BBB", () => {
      functions
      const response = "BBB"
      text(response)
    })
  })

  // describe("the add method works ok", () => {
  //   it("should add two numbers together", () => {
  //     const num1 = 20;
  //     const num2 = 30;
  //     const obj = new Calculator(num1, num2);
  //     expect(obj.add()).toEqual(50);
  //   })
  // })
})